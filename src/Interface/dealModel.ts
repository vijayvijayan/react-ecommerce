export interface dealMpdel {
  id: number;
  dealTypeName: string;
  startDate: string; // ISO 8601 date string
  endDate: string;   // ISO 8601 date string
  // activeFlag: boolean;
  // createdDate: string; // ISO 8601 date string with time
  // createdBy: string;
  // applicationUser: null | unknown;
}